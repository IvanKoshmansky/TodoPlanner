<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TodoPlanner" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <!-- action bar, цвет фона кнопок, цвет текста text button -->
        <item name="colorPrimary">@color/teal</item>
        <!-- status bar -->
        <item name="colorPrimaryVariant">@color/teal_dark</item>
        <!-- цвет текста, который выводится поверх colorPrimary -->
        <item name="colorOnPrimary">@color/white</item>
        <!-- поверхность карточек -->
        <item name="colorSurface">@color/yellow_light</item>
        <!-- Secondary brand color. -->
        <!-- FAB -->
        <item name="colorSecondary">@color/yellow</item>
        <!-- цвет текста поверх colorSecondary -->
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/mark_pro</item>
        <item name="background_drawable">@drawable/green_background_drawable</item>
    </style>
</resources>